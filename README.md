up frontend service:
npm run dev -- --port 3001

up backend service:
npm run start:dev

up backend prisma studio:
npx prisma studio

Frontend Nuxt 3   → http://localhost:3001
Backend NestJS    → http://localhost:3000 
PostgreSQL Docker → localhost:5432 
Prisma Studio     → http://localhost:5555 